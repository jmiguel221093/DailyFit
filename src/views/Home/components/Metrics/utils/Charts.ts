import { scaleSize, color } from '../../../../../styles';

export default {
    height: scaleSize(50),
    width: scaleSize(108),
    interpolation: 'natural',
    padding: 0,
    strokeWidth: scaleSize(2),
    positiveColor: color('primary'),
    negativeColor: color('danger'),
};
