import { StyleSheet } from 'react-native';
import { spacing, color, radius } from '../../styles';

export default StyleSheet.create({
    container: {
        padding: spacing('extraLoose'),
        backgroundColor: color('black', '90'),
        borderRadius: radius('huge'),
    },
});
